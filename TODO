inline functions

Add a zip library made with [this](https://github.com/PlutoLang/pluto-zip)

add `sync` to pair with `with`
  `sync` will make it so operations between each will be updated
  ex:
  ```
  int one = 32;
  sync with {
    one++;
  } and {
    one++;
  }
  ```
  This should also use atomics :)

Add hive variant of list (like std::hive)

Standardize fail and pass return

Add compiler grouping
Add personal scopes

64KB sized packets for custom TCP

More keywords
case
struct
enum
async

let file = open("test.txt") ? throw NameError("File missing!") : print(file);

add a str_start to strings, and str_start
