#pragma once
enum u16 ErrorCatches {
  IndexOutOfBounds,
  Unknown,
  IntegerOverflow,
  DoubleOverflow,
  UnknownTrace
}

struct Catch {
  ErrorCatches ErrorToCatch,
  list ToDo
};

struct TryCatch {
  // the try
  list ToDo,
  // the catch(s)
  list[][Catch]
};
