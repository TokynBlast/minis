#pragma once
enum u16 ErrorCatches {
  OutOfBounds,
  Unexpeted,
  UseAfterFree, // we try to minimize these
}

struct TryCatch {
  // the try
  list ToDo,
  // the catch(s)
  list[][list[Catch]]
};

struct Catch {
  ErrorCatches ErrorToCatch,
  list ToDo
};
