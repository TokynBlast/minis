#pragma once
enum u16 ErrorCatches {
  IndexOutOfBounds,
  Unknown,
  IntegerOverflow,
  DoubleOverflow,
  UnknownTrace
}

struct TryCatch {
  // the try
  list ToDo,
  // the catch(s)
  list[][list[Catch]]
};

struct Catch {
  ErrorCatches ErrorToCatch,
  list ToDo
};
