/// Clamp value a and b
void* clamp(void* num, void* min, void* max) {
  if (min > max) {
    error("min cannot be greater than max!")
  }
  num = {
    mum < min -> min;
    num > max -> max;
    _ -> num;
  };
}
